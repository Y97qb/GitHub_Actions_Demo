name: example-file-upload

on:
  push:
    branches:
      - feature/nxy-45

jobs:
  upload_file:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Upload File to SharePoint
        shell: pwsh
        run: |
          pwsh -File ./execute-data-patch.ps1
